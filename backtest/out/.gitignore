# Regenerated by `python3 backtest/run_backtest.py`. The two per-selection
# logs are large and derivable; the summaries beside them are committed so a
# reviewer can read the result without running anything.
fixtures.csv
bets.csv
